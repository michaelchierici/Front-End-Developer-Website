export interface ThemeType {
  colors: {
    background: string;
    menu_background: string;

    gradient: string;
    text_gradient: string;

    text_primary: string;
    text_secondary: string;

    button: string;
    font: {
      size: number;
      weight: number;
    };
  };
}
